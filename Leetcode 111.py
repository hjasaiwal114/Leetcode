"""
111. Minimum  Depth of Binary Tree

Given a binary, find its minimum depth.
The minimum depth is the number if nodes along the shortest path from the root down to the nearest lead node.

Note: A leaf is a node with no children.
"""

class Solution:
    def minDepth(self, root: Optional[TreeNode]) -> int:
        